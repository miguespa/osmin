# Los scripts de instalación de las dependencias no se ejecutan.
# El build no los necesita —esbuild distribuye su binario por optionalDependencies—
# y los postinstall son el vector clásico de los compromisos de paquetes npm.
# Verificado: `npm run build` sigue funcionando con esta opción activa.
ignore-scripts=true
